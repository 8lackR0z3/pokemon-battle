import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blackrose.pokemonbattle',
  appName: 'Pokemon Battle',
  webDir: 'build'
};

export default config;
